/**
 * HumanHugGalleryDemo Page
 * 
 * This page showcases the human-figured hug icons.
 * Extremely simplified version to avoid circular dependencies.
 */

import SimpleHugGallery from './SimpleHugGallery';

// Export the SimpleHugGallery component directly to avoid any potential problems
export default SimpleHugGallery;